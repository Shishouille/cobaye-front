import styled from 'styled-components';
import theme from 'src/styles/theme';

const StyledParticipation = styled.div`
  margin-left: 1em;
  margin-right: 1em;
  h1 {
    margin-top: 1em;
  }
  h2 {
    margin: 1em 0;
  }
  .participation-textarea {
    display: flex;
    flex-direction: column;
    margin-bottom: 1em;
    }
 `;

export default StyledParticipation;